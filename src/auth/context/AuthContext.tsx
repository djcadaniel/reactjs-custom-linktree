//Este archivo proveerá toda la informacion a toda mi aplicacion.
import { createContext } from "react";

// export const AuthContext = createContext({} as any);
export const AuthContext = createContext({} as any);